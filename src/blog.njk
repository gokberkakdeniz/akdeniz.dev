---
layout: default
title: Blog
description: The blog of Gökberk Akdeniz
keywords: blog, software engineer, izmir, computer engineer, programming
pagination:
  data: collections.post
  reverse: true
  size: 10
  alias: posts
---
<div>
  <ul>
    {% for post in posts %}
      <li class="hover:bg-secondary border border-dotted border-transparent hover:border-accent p-2">
        <a href="{{post.url}}">
          <div class="flex flex-row">
            <div class="flex-grow">
              <b class="font-serif">{{post.data.title}}</b>
            </div>
            <div class="flex-none">
              <b>{{post.data.date | dateFormat}}</b>
            </div>
          </div>
          <p>{{ post.data.description }}</p>
        </a>
      </li>
    {% endfor %}
  </ul>
  <div class="flex flex-row justify-between">
    <div class="py-1 px-2 hover:text-accent">
      {% if pagination.href.previous %}
        <a href="{{ pagination.href.previous }}">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" class="h-4 w-4 inline-block">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
          </svg>
          Previous
        </a>
      {% endif %}
    </div>

    <div class="py-1 px-2 hover:text-accent">
      {% if pagination.href.next %}
        <a href="{{ pagination.href.next }}">
          Next
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" class="h-4 w-4 inline-block">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
          </svg>
        </a>
      {% endif %}
    </div>
  </div>
</div>